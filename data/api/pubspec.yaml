name: api
description: api module
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  dio: ^4.0.0
  freezed_annotation: ^0.14.2
  riverpod: ^0.14.0+3

  common:
    path: ../../common

dev_dependencies:
  build_runner: ^2.0.6
  freezed: ^0.14.2
  json_serializable: ^4.1.4
