name: data_repository
description: data_repository module
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  riverpod: ^0.14.0+3

  common:
    path: ../../common
  api:
    path: ../api
  mapper:
    path: ../mapper
  model:
    path: ../../domain/model
  domain_repository:
    path: ../../domain/repository
